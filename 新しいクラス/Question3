問
twiceメソッドを実行する前と実行した後で、変数array[0]は値が変わるが、
変数numは値が変わらないのはなぜか。

変数array[0]は参照型
実行前は10になっているが参照型なので10がある番地（参照先）を分かっていてそこから持ってきているだけ。
実行すると番地（参照先）にある10を20に変える
次に呼び出す時には番地（参照先）を知っているだけなので20に変わったままになる

変数numは基本データ型
値自体を持っているので最後は10になる